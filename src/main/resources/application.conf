ktor {
    development = true
    deployment {
        port = 8080
        port = ${?PORT}
        watch = [ classes, resources, plugins ]
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
}
